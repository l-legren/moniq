/**
 * Static profile for the MVP (no auth yet). Used by the Today header and Settings.
 * Names/emails are data, not copy, so they don't go through i18n.
 */

export const PROFILE = {
  name: 'Mara',
  email: 'mara@example.com',
  initial: 'M',
} as const;
